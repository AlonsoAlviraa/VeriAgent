steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - europe-west1-docker.pkg.dev/$PROJECT_ID/verifleet/verifleet:ata
      - -f
      - infra/Dockerfile.cloudrun
      - .
images:
  - europe-west1-docker.pkg.dev/$PROJECT_ID/verifleet/verifleet:ata
timeout: 600s
