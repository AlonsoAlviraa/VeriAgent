# Social draft — publish on X or LinkedIn (public, not unlisted)

Use this text as-is or shorten. The hashtag must stay `#AllThingsAgenticHackathon` (no space). Do not invent a Cloud Run URL; add it only after deploy.

## X / LinkedIn

VeriFleet — a Fortified Enterprise Fleet for Spanish VeriFactu.

Drop invoices. Agents audit, sign, or escalate in the background. Not a chatbot.

Gemini 3.5 (`gemini-3.5-flash`) consults via Google ADK `InMemoryRunner`. It can tighten SIGN → ESCALATE. It cannot loosen a failed gate. It never writes the hash. A disclosed pre-existing kernel does.

Built for All Things Agentic 2026. Repo + 5-minute clone (SQLite `DATABASE_URL`) in the README.

#AllThingsAgenticHackathon

## Optional shorter X variant

A Spanish SME’s afternoon: VAT, tax IDs, VeriFactu chaining. VeriFleet does it without a chat loop. Gemini 3.5 + Google ADK. The LLM never writes the hash.

#AllThingsAgenticHackathon
